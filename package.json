{
  "name": "clarus",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.cjs",
    "dev": "node scripts/dev.cjs",
    "dev:be": "node scripts/dev.cjs --backend-only",
    "dev:fe": "cd FE && npm run dev",
    "chrome:debug": "node scripts/chrome-debug.cjs",
    "db:up": "docker compose -f BE/docker-compose.yml up -d",
    "db:down": "docker compose -f BE/docker-compose.yml down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prisma": "^6.19.2"
  },
  "dependencies": {
    "@prisma/client": "^7.4.0"
  }
}
